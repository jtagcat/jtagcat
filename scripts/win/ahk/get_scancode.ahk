#InstallKeybdHook
a::a
;You can see the scan code and the virtual key code of all keys you can type with AutoHotkey.
;Run a script with keyboard hook (may be just #InstallKeybdHook),
;open the AutoHotkey window (context menu in systray, Open item),
;go to View > Key History and script info (Ctrl+K) and type some keys.
;Hit F5 to see the codes.
